version: "3.8"
services: 
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - redis-net
    volumes:
      - redis-data:/data
networks:
  redis-net:

volumes:
  redis-data: